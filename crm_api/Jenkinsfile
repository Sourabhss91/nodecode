pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'GitHub webhook is working!'
                sh 'hostname'
                sh 'date'
            }
        }
    }
}